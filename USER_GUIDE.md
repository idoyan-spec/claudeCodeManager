# Claude Code Manager — חוברת הסבר למשתמש

> מסמך זה נכתב עבורך. אין צורך בידע טכני או באנגלית — כל שלב מוסבר מההתחלה.
> **גרסה:** 2026-07-23 23:12 v23 file-browser

## מה הכלי הזה עושה? (בשפה פשוטה)
דמיין שכל פרויקט שאתה עובד עליו הוא **עובד** שאתה נותן לו משימות. עד היום כל עובד
ישב בחדר נפרד (חלון טרמינל נפרד), והיית צריך לרוץ בין החדרים כדי לבדוק מי סיים ומי
עוד עסוק. וכשהיו יותר מחמישה חדרים — השלטים על הדלתות הצטמצמו עד שלא יכולת לקרוא
אותם.

הכלי הזה מכניס את **כל העובדים לחדר אחד**, ושם להם **שלט ברור בצד** עם השם של כל
אחד ונורית שמראה אם הוא **עובד עכשיו**, **מחכה לך**, או **צריך אותך דחוף**. חדר אחד,
רשימה מסודרת בצד, ומבט אחד אומר לך הכל.

## איך מתחילים?

### הדרך הקלה — פקודה אחת שמתקינה הכל (וגם מעדכנת)
במחשב חדש, או כשרוצים לעדכן למחשב הזה את כל מה שהוספנו — פותחים PowerShell ומריצים:

```powershell
git clone https://github.com/idoyan-spec/claudeCodeManager.git
cd claudeCodeManager
powershell -ExecutionPolicy Bypass -File .\scripts\bootstrap.ps1
```

`bootstrap.ps1` עושה הכל בבת אחת: מתקין את **Claude עצמו** אם הוא חסר, את הפקודה
`ccm`, את הגדרות VS Code, ההוקים, **התוסף** (כולל כפתור ה-PDF), **ואת כלי ההכתבה
הקולית** (מחזיקים Ctrl ימני ומדברים — מוסבר בהמשך). הוא **שומר גיבויים**
של ההגדרות הקודמות, אז אין סיכון. **הרצה חוזרת שלו = עדכון** לכל מה שהוספנו מאז
(הוא מושך קודם את הגרסה האחרונה מ-GitHub). בסוף — **טען מחדש חלון VS Code**.

שלוש נקודות למחשב שאינו של עידו:
- שני המאגרים **ציבוריים** (רישיון MIT) — לא צריך חשבון GitHub ולא שום התחברות
  כדי להתקין ולהתעדכן. לשנות אותם יכול רק עידו.
- המתקין ישאל אתכם **פעם אחת** אם תרצו להדביק **מפתח Gemini משלכם** (בחינם,
  https://aistudio.google.com/apikey) — הוא נדרש רק לכרטיס "הסבר לי את הטקסט המסומן";
  ההכתבה הקולית עובדת גם בלעדיו, כי התמלול כולו מקומי. המפתח נשמר **מוצפן** במחשב
  שלכם בלבד, אף פעם לא בקובץ ולא בגיט.
- מאותו רגע המחשב **מתעדכן לבד**: משימה שקטה מושכת את הגרסה האחרונה בכל כניסה
  למחשב וכל 12 שעות. מדריך מלא לבעל מחשב חדש: `docs/INSTALL.md`.

### הדרך הידנית (רק ההגדרות, בלי Claude/התוסף)
1. פתח PowerShell.
2. הדבק את השורה הבאה ולחץ Enter:
   ```powershell
   E:\MAIN_CLAUDE\claudeCodeManager\scripts\install.ps1
   ```
   זה מכין את הפקודה `ccm` ואת ההגדרות של VS Code. הוא **שומר גיבוי** של ההגדרות
   הקודמות שלך, אז אין סיכון.
3. **סגור ופתח מחדש חלון VS Code** — זה חשוב, אחרת השינוי לא נכנס לתוקף.

מהרגע הזה, החלון של VS Code הוא ה"חדר המשותף" שלך.

### התקנה נוספת (מומלצת): כניסה בלחיצה ימנית מ-Explorer
כדי שתוכל לפתוח סשן חדש **בלי להקליד כלום** — רק לחיצה ימנית על תיקייה — התקן פעם
אחת את התוסף ואת תפריט הלחיצה-הימנית:

1. פתח PowerShell והדבק:
   ```powershell
   powershell -ExecutionPolicy Bypass -File E:\MAIN_CLAUDE\claudeCodeManager\ccm-extension\install-extension.ps1
   powershell -ExecutionPolicy Bypass -File E:\MAIN_CLAUDE\claudeCodeManager\explorer-context-menu\install-context-menu.ps1
   ```
2. **טען מחדש את VS Code** (או פתח חלון חדש) כדי שהתוסף יתחיל לעבוד.

אין צורך בהרשאות מנהל, ואין תוכנה כבדה — התוסף הוא קובץ קטן שמועתק לתיקיית
התוספים שלך.

## שלוש הדרכים לפתוח סשן

| דרך | איך | מתי |
|------|------|------|
| **`Alt+O` (הכי מהיר)** | בתוך VS Code לחץ `Alt+O` → נפתח **חלון צף** עם כל הפרויקטים → חצים או הקלדה לסינון → `Enter` | הדרך היומיומית. לא צריך לזכור או להקליד נתיב |
| **לחיצה ימנית** | Explorer → קליק ימני על תיקייה → **"Open in Claude Code (VS Code)"** | כשאתה כבר ב-Explorer ורואה את התיקייה |
| **הקלדה** | בטרמינל של VS Code: `ccm <נתיב>` | לתיקייה שאינה תחת `E:\MAIN_CLAUDE` |

יש גם **"Open in Claude Code (Auto)"** — זה פותח חלון טרמינל **נפרד** (Windows
Terminal) עם Claude, למי שרוצה סשן בודד בלי החלון המאוחד.

### החלון הצף (`Alt+O`) — מה רואים בו

הרשימה מסודרת **לפי מתי עבדת בכל פרויקט לאחרונה** — העדכני ביותר למעלה. לצד כל שם
כתוב מתי זה היה (`just now`, `2h ago`, `5d ago`), ותיקייה שמעולם לא הרצת בה Claude
כתוב לידה `never opened` והיא יורדת לתחתית הרשימה.

- **פרויקט שכבר פתוח** מסומן `● running`, ובחירה בו פשוט **קופצת אליו** במקום לפתוח
  אותו פעם שנייה.
- **כמה משימות מחכות שם** — אם לפרויקט יש קובץ משימות (`שם-התיקייה-TASKS.md`),
  יופיע לידו סמל רשימה עם מספר: כמה משימות **פתוחות** יש בו. כמו פתקית על דלת של
  חדר שאומרת כמה דברים מחכים בפנים — עוד לפני שנכנסת. משימה שסומנה `✓` לא נספרת,
  ותת-הערות מוזחות מתחת למשימה נחשבות חלק ממנה. אין מספר? אין שם כלום שמחכה.
- אפשר פשוט **להקליד כמה אותיות** מהשם כדי לסנן.
- `Esc` סוגר בלי לפתוח כלום.
- הבחירה פותחת טרמינל חדש בתיקייה, מריצה את Claude, והחלון הצף נסגר מעצמו.

הרשימה מציגה את התיקיות שנמצאות ישירות בתוך `E:\MAIN_CLAUDE`. אם תרצה תיקיית-אב
אחרת, שנה ב-VS Code את ההגדרה `ccmHub.projectsRoot`.

> **חשוב:** אחרי התקנה של גרסה חדשה של התוסף צריך **לטעון מחדש** את VS Code
> (`Ctrl+Shift+P` → `Reload Window`), אחרת `Alt+O` עדיין לא מחובר.

## לסגור סשן בלי לאבד אותו

כל טאב בטרמינל הוא **שיחה חיה עם Claude**. קליק אחד לא-מכוון על פח האשפה היה
מספיק כדי לסיים אותה. עכשיו שני שומרים עומדים בדרך.

### `Alt+Q` — הדרך הנכונה לסגור

לחץ `Alt+Q` (או קליק ימני על הטאב → `ccm: Close terminal`), ותקפוץ שאלה עם **שלוש**
תשובות. השאלה **תמיד אומרת בשם איזו תיקייה מדובר** — קרא אותה לפני שאתה עונה.

| הכפתור | מה קורה |
|---------|----------|
| **גבה וסגור** | מריץ `/session-backup` בתוך הסשן, **מחכה שהגיבוי יסתיים**, ורק אז סוגר את הטאב |
| **סגור** | סוגר עכשיו, בלי גיבוי |
| **השאר פתוח** | לא קורה כלום. גם `Esc` וגם ה-`X` של החלונית פירושם זה |

"גבה וסגור" סוגר את הטרמינל **רק במקרה אחד**: הגיבוי הסתיים בהצלחה. אם הוא נתקע,
אם ביטלת, או אם Claude עצר כדי לשאול אותך משהו (`‼`) — **הטרמינל נשאר פתוח** ותקבל
הודעה שמסבירה למה. הכלל הוא פשוט: כלי שתפקידו למנוע אובדן טרמינל לא יאבד לך טרמינל.

תוך כדי הגיבוי תראה בפינה הימנית-תחתונה חיווי התקדמות עם כפתור **Cancel**. ביטול
משאיר את הטרמינל פתוח כמו שהיה.

### הפח (🗑) — ומה קורה אם בכל זאת נסגר

את כפתור הפח של VS Code **אי אפשר לתפוס** מתוך תוסף — VS Code פשוט לא מודיע לתוסף
"הטרמינל עומד להיסגר". לכן במקום זה:

- **VS Code עצמו שואל** לפני הריגה של טרמינל שרץ בו תהליך. התוסף מדליק את זה בשבילך
  (ברירת המחדל של VS Code שאלה רק על טרמינלים שנפתחו באזור העורך — כלומר לא על שלנו).

> **שאלה אחת, לא שתיים.** בגרסאות `v17`–`v21` סגירת חלון עם טרמינלים חיים שאלה
> אותך **פעמיים** ברצף (פעם על החלון, פעם על הטרמינלים) — שני שומרים על אותה דלת.
> מגרסה `v22` נשאר שומר אחד: כל סגירת חלון שואלת שאלה אחת בלבד.
  זו שאלה של כן/לא, בלי אפשרות גיבוי; בשביל הגיבוי יש `Alt+Q`.
- **ואם סשן בכל זאת נסגר בטעות** — תקפוץ הודעה עם הכפתור **"שחזר את השיחה"**. לחיצה
  עליו פותחת מחדש את התיקייה עם `claude --continue`, וה**שיחה חוזרת בדיוק מהמקום
  שבו הייתה**. Claude שומר את התמלול על הדיסק, אז מה שנהרג הוא התהליך — לא השיחה.

> רוצה לכבות את כל ההגנות האלה? ב-VS Code, הגדרה `ccmHub.guardTerminalClose` →
> `false`. `Alt+Q` ימשיך לשאול בכל מקרה.

## סייר הקבצים הצף — `Alt+E`

לחיצה על `Alt+E` פותחת **חלון צף בסגנון Explorer** מעל מה שאתה עושה: **עץ התיקיות
בשמאל, תוכן התיקייה בימין**. הוא נסגר ב-`Esc`, בכפתור ה-`✕`, או **ברגע שאתה נוגע
במקום אחר** (טרמינל, קובץ, טאב אחר) — בדיוק כמו חלון צף אמיתי.

### הכל מהמקלדת, בלי לגעת בעכבר
| מקש | מה קורה |
|------|----------|
| `Tab` | מעבר בין העץ (שמאל) לרשימה (ימין) |
| `↑` / `↓` | מעלה/מטה ברשימה או בעץ |
| `→` בעץ | פותח תיקייה. `←` — סוגר, ואם היא כבר סגורה — עולה לתיקיית האב |
| `→` ברשימה | נכנס לתיקייה המסומנת. `←` — קופץ חזרה לעץ |
| `Enter` | תיקייה — נכנס אליה; קובץ — **פותח אותו בעורך** |
| `F5` או `Ctrl+Enter` | **מריץ** את הקובץ המסומן |
| `Backspace` | תיקייה אחת למעלה |
| `Alt+←` / `Alt+→` | אחורה/קדימה בהיסטוריית הניווט |
| הקלדת אותיות | קפיצה לפריט שמתחיל באותיות שהקלדת (בדיוק כמו ב-Explorer) |
| `Ctrl+F` | תיבת סינון. `Esc` מנקה אותה |
| `Ctrl+H` | מציג/מסתיר קבצים מוסתרים (`.git`, `.env`…) |
| `F2` / `Delete` | שינוי שם / מחיקה (לסל המיחזור) |
| `Shift+F10` או מקש-התפריט | פותח את **התפריט הימני** על הפריט המסומן — וגם בו זזים בחצים ובוחרים ב-`Enter` |

### להריץ קובץ
לכל קובץ שאפשר להריץ (`.ps1`, `.js`, `.py`, `.bat`, `.exe`, `.sh`, `.go`…) יש **▶
קטן בשורה** — לחיצה עליו פותחת טרמינל בתיקייה של הקובץ ומריצה אותו. אותו דבר עושים
`F5` ו-`Ctrl+Enter`. קובץ שאין לו מריץ מוכר נפתח בתוכנת ברירת המחדל של Windows,
בדיוק כמו דאבל-קליק ב-Explorer.

> **למה `Enter` פותח ולא מריץ?** כדי ש"סתם דפדוף" לא יריץ לך בטעות סקריפט. אם אתה
> מעדיף ש-`Enter` **יריץ** — בהגדרות VS Code: `ccmHub.browser.enterRuns` → `true`.

### לפתוח טרמינל של סוכן AI בתוך כל תיקייה
בתפריט הימני יש **🤖 סוכן AI** — ובו כל הסוכנים: **Claude Code, Codex, Gemini,
GitHub Copilot, Cursor Agent, opencode, Qwen Code, Aider**. בחירה פותחת טרמינל
**בתיקייה שעליה עמדת** ומריצה שם את הסוכן.

- סוכן שמותקן במחשב מופיע רגיל; סוכן שלא מותקן מופיע **מעומעם** עם הכיתוב *לא
  מותקן*, ולחיצה עליו מציעה **להתקין אותו** בלחיצה אחת (או *להריץ בכל זאת*, למקרה
  שהתקנת אותו אחרי שחלון VS Code נפתח — אז VS Code עדיין לא "רואה" אותו עד ריענון).
- **Claude נפתח דרך אותו מנגנון של `Alt+O`** — כלומר עם כל ההגנות: שומר הסגירה,
  הצעת השחזור, ו"כבר רץ כאן → פשוט תקפוץ אליו".
- רוצה סוכן שלא ברשימה? הגדרה `ccmHub.browser.agents` מקבלת רשימה משלך.

### מה עוד יש בתפריט הימני
פתיחה בעורך · הרצה · פתיחה בתוכנת ברירת המחדל · **ייצוא ל-PDF (RTL)** לקבצי `.md` ·
טרמינל רגיל בתיקייה · פתיחת התיקייה **כפרויקט בחלון VS Code חדש** · הצגה ב-Explorer
של Windows · העתקת נתיב מלא / שם · קובץ חדש · תיקייה חדשה · שינוי שם · מחיקה · רענון.

> **מחיקה בטוחה:** כל מחיקה הולכת ל**סל המיחזור**, לא נמחקת לצמיתות. שינוי שם
> לעולם לא ידרוס קובץ קיים.
>
> **צריך לטעון מחדש את VS Code** פעם אחת אחרי העדכון כדי ש-`Alt+E` יתחיל לעבוד.

## לייצא קובץ Markdown ל-PDF עם עברית מיושרת ימין

יש לך קובץ `.md` בעברית ואתה רוצה ממנו PDF תקין — עם היישור לימין? 

1. פתח את הקובץ ב-VS Code.
2. למעלה-מימין, בסרגל-הכלים של הקובץ, יש עכשיו **כפתור PDF** (📄). לחץ עליו.
   (אפשר גם: לחיצה **ימנית** על הקובץ ב-Explorer → *ccm: Export Markdown to PDF (RTL)*.)
3. נוצר קובץ `<שם-הקובץ>.pdf` **לצד** ה-MD, ותקבל הודעה עם כפתור **"פתח PDF"**.

**עברית מזוהה אוטומטית** ומיושרת לימין; מסמך באנגלית יישאר משמאל; קטעי קוד תמיד
משמאל-לימין. אין צורך להתקין שום דבר — הייצוא משתמש ב-Edge/Chrome שכבר על המחשב.

> למה זה היה חסר: ה-Preview של VS Code מציג עברית נכון אבל **אי אפשר לייצא ממנו**,
> ולעורך-הקוד עצמו אין ב-VS Code מצב RTL בכלל — אז עד עכשיו לא הייתה דרך לקבל PDF
> עברי מיושר-ימין. הכפתור הזה סוגר בדיוק את הפער הזה.
>
> **צריך לטעון מחדש את VS Code** פעם אחת אחרי העדכון כדי שהכפתור יופיע.

## סיור על המסכים והכפתורים

### חלון VS Code (ה"האב")
- **רשימת הטאבים בצד שמאל** — כאן מופיע שלט לכל סשן פתוח. כל שורה = פרויקט אחד,
  עם ריבוע-מודל, נורית סטטוס ושם התיקייה. הרשימה **לא מצטמצמת** גם אם יש בה עשרים שורות.
- **הטרמינל (החלק התחתון/הצדדי)** — כאן באמת רואים את Claude עובד. לחיצה על שורה
  ברשימה בצד מעבירה אותך לסשן הזה.
- **כפתור `+` / `Ctrl+Shift+5`** — פותח טרמינל חדש (עובד חדש) באותו החדר.

### הפקודה `ccm`
זו כל מה שאתה צריך לזכור. במקום לפתוח חלון חדש לכל פרויקט, אתה כותב:
```
ccm <נתיב-התיקייה>
```
והכלי: נכנס לתיקייה, נותן לטאב את שם התיקייה, ומריץ את Claude. זהו.

### מה כתוב על הטאב

כל שורה ברשימה נראית כך: **`<ריבוע-מודל> <נורית-סטטוס> <שם-תיקייה>`**
לדוגמה: `🟨 ⟳ claudeCodeManager` — סשן Opus שעובד כרגע.

**נוריות הסטטוס** — כמו רמזור על דלת החדר:

| מה רואים | מה זה אומר | מה לעשות |
|----------|-----------|----------|
| `⟳` | **עובד** — Claude עסוק במשימה | אפשר ללכת לטפל בסשן אחר |
| `✓` | **התור שלך** — Claude סיים וממתין (גם סשן חדש שטרם דיברת איתו) | חזור והמשך לשוחח |
| `‼` | **דורש אותך** — Claude מבקש אישור/החלטה (גם צליל) | טפל בזה עכשיו כדי שלא ייתקע |

`‼` מופיע **רק** כשבאמת מבקשים ממך משהו. אם סשן פשוט מחכה לך בשקט הוא נשאר `✓`,
גם אחרי שעה. (בגרסאות קודמות כל טאב שנח דקה הפך ל-`‼`, וזה רוקן את הסימן ממשמעות.)

**והנורית לא משקרת גם בזמן "דחיסת זיכרון" (COMPACT).** כששיחה מתארכת, Claude מכווץ
מדי פעם את הזיכרון שלה כדי להמשיך לעבוד — לגמרי לבד, בלי שתצטרך לעשות כלום. עד
גרסה `v22` הרגע הזה הצית `✓` כאילו העבודה נגמרה, למרות ש-Claude באמצע משימה.
עכשיו: דחיסה **אוטומטית** באמצע עבודה משאירה `⟳`, ורק `/compact` שהקלדת בעצמך
חוזר ל-`✓`. המשמעות ללילות: אפשר להשאיר את Claude לעבוד לבד כל הלילה — הדחיסות
קורות מעצמן, והנוריות על הטאבים יראו בבוקר את האמת.

**ריבוע המודל** — באיזה מודל הסשן רץ:

| ריבוע | מודל |
|-------|------|
| `🟨` | Opus |
| `🟦` | Fable |
| `🟥` | Haiku |
| `🟩` | Sonnet |

הריבוע **מתעדכן לבד** כשאתה מחליף מודל עם `/model`, והוא מופיע **כבר ברגע שהסשן נפתח**.

> **למה הריבוע ולא צבע-רקע של הטרמינל?** VS Code מקפיא את הצבע והאייקון של טרמינל
> ברגע שהוא נוצר, ואי אפשר לשנות אותם אחר כך. בנוסף, Claude Code לא שומר בשום קובץ
> על איזה מודל אתה. הכותרת היא המקום היחיד שיכול לעקוב אחרי `/model` בזמן אמת.

### איזה טאב פעיל עכשיו?
לטאב שאתה עומד עליו יש **קו אדום זוהר** בקצה, ולרוב גם **רקע כחול מלא**.

> **למה שניים?** הקו האדום קשור לטרמינל **הפעיל** — הוא אף פעם לא נעלם. הרקע הכחול
> קשור ל"שורה המסומנת ברשימה", וזה משהו אחר: אם תלחץ על השטח **הריק** מתחת לטאבים,
> VS Code מבטל את הסימון והרקע ייעלם — אבל הקו האדום יישאר, ולכן תמיד תדע איפה אתה.
>
> VS Code לא מאפשר לצבוע רקע של טאב-טרמינל בנפרד, ולא מאפשר לעבות את הקו. הרקע מגיע
> מצבע ה"שורה הנבחרת" הכללי, ולכן **גם הקובץ המסומן ב-Explorer יקבל את אותו כחול**.

### הטאב מהבהב באדום כשצריך אותך
כשסשן **מבקש ממך אישור**, או כש**סיים וממתין לך כבר דקה**, מופיע על הטאב שלו
**אייקון פעמון אדום** לשלוש שניות. רק על הטאב שלו — לא על כולם.

זה עובד כי Claude מצלצל בפעמון הטרמינל (`preferredNotifChannel: terminal_bell`),
ו-VS Code הופך צלצול לאייקון זמני על הטאב (`enableVisualBell`).

> **חשוב:** ההגדרה נקראת רק כשסשן **נפתח**. סשנים שכבר רצים לא יהבהבו — סגור ופתח
> אותם מחדש.

### מעבר בין סשנים — הסמן תמיד מוכן להקלדה
פעם היית לוחץ על טאב, ואז נאלץ ללחוץ **עוד פעם** בתוך הטרמינל כדי להתחיל לכתוב.
זה נגמר. שלוש דרכים, לפי איפה אתה נמצא:

| איפה אתה | מה ללחוץ | מה קורה |
|----------|----------|---------|
| מצביע על טאב עם העכבר | **לחיצה אחת** | נכנסת לסשן, הסמן כבר בשורת ההקלדה |
| הסמן ברשימת הטאבים בצד | **↑ / ↓** | עובר לסשן, נכנס אליו, והסמן בשורת ההקלדה |
| אתה כבר כותב בטרמינל | **Ctrl+↑/↓** *או* **Alt+↑/↓** | קופץ לסשן הקודם/הבא, בלי לעזוב את שורת ההקלדה |

**למה שתי קבוצות מקשים ולא אחת?** כי הסמן ("פוקוס") הוא כמו האצבע שלך — הוא יכול
להיות רק במקום אחד. אם החץ מקפיץ אותך לשורת ההקלדה, אז החץ הבא כבר "שייך" ל-Claude
(הוא מעלה לך פרומפט קודם מההיסטוריה). לכן ברגע שאתה בתוך טרמינל, המעבר בין סשנים
דורש מקש-צירוף.

**גם `Ctrl` וגם `Alt` עובדים** (מגרסה `v11`) — שניהם מחוברים לאותה פעולה, כדי שלא
תצטרך לזכור. `Ctrl+↑/↓` תפסו קיצור ישן של VS Code (קפיצה בין פלטים של פקודות קודמות),
שממילא לא עושה כלום בתוך Claude; `Alt+↑/↓` היו פנויים בטרמינל.

> אגב, VS Code מגיע מהיצרן עם `Ctrl+PageUp` / `Ctrl+PageDown` לאותה מטרה בדיוק.
> לא נגענו בהם — הם עדיין עובדים.

### לכתוב הודעה של כמה שורות — `Shift+Enter`
כשאתה כותב ל-Claude הודעה ארוכה, לפעמים רוצים לרדת שורה **בלי לשלוח** — כמו
בוואטסאפ, שבו Enter שולח ו-`Shift+Enter` יורד שורה. עכשיו זה עובד גם כאן:

| מה ללחוץ | מה קורה |
|----------|---------|
| `Enter` | שולח את ההודעה ל-Claude (כרגיל) |
| `Shift+Enter` | יורד שורה חדשה **בתוך** ההודעה, בלי לשלוח |

זה עובד בכל טרמינל בתוך VS Code, ואין צורך בריענון — הקיצור פעיל מיד.

### הקבצים נשמרים לבד (ואפשר לכתוב בזמן ש-Claude עובד)

מגרסה `v22` VS Code שומר קבצים **אוטומטית** כשנייה אחרי שהפסקת להקליד
(`files.autoSave: afterDelay`). למה זה חשוב כאן? כי כל הרעיון הוא שאתה כותב
בקובץ המשימות **בזמן ש-Claude עובד**. Claude יודע לזהות שינויים שהגיעו לדיסק
ולא דורס אותם — אבל שינוי שנשאר רק על המסך (הנקודה ליד שם הקובץ) הוא בלתי-נראה
לו. שמירה אוטומטית מזרימה כל שורה שכתבת לדיסק מיד, כמו מחברת שמצלמת כל דף ברגע
שסיימת לכתוב אותו — ודיאלוג ה"Overwrite" המפחיד פשוט לא יופיע יותר.

אם הגדרת בעצמך `files.autoSave` (כל ערך, כולל כיבוי) — הכלי מכבד ולא נוגע.

### הטרמינל יושב למעלה
מגרסה `v12` חלון הטרמינל עובר ל**חלק העליון** של VS Code, כדי שהסשנים יהיו הדבר
הראשון שאתה רואה ולא רצועה דקה בתחתית המסך.

זה קורה **פעם אחת לכל תיקייה** שאתה פותח. אם אתה מעדיף אותו למטה — פשוט גרור אותו
חזרה, והכלי לא יזיז אותו שוב **באותה תיקייה**.

> **למה "לכל תיקייה" ולא פעם אחת?** VS Code זוכר את מיקום החלון התחתון **בנפרד לכל
> תיקייה שפתחת**, כמו שכל חדר בבית זוכר איפה שמת בו את הרהיטים. עד `v11` הכלי סימן
> לעצמו "כבר הזזתי" סימון אחד גלובלי, ולכן הזיז את החלון רק בתיקייה הראשונה — ובכל
> השאר הוא נשאר למטה בלי הסבר.

## תרחיש לדוגמה (Case Study) — מהתחלה ועד הסוף
נניח שאתה עובד במקביל על שני פרויקטים: אתר (`E:\sites\shop`) ובוט (`E:\bots\wa`).

1. פתח חלון VS Code (החדר המשותף). פתח טרמינל.
2. הקלד `ccm E:\sites\shop` ולחץ Enter. נפתח סשן, ובצד שמאל מופיעה שורה: `🟨 ✓ shop` —
   הריבוע והנורית מופיעים **מיד**, עוד לפני הפרומפט הראשון.
3. בקש מ-Claude משהו ארוך, למשל "תבנה עמוד תשלום". השורה בצד משתנה ל-`🟨 ⟳ shop` —
   כלומר הוא עובד. אתה **לא צריך לחכות** לו.
4. פתח טרמינל שני (`Ctrl+Shift+5`), הקלד `ccm E:\bots\wa`. עכשיו יש שתי שורות בצד:
   `🟨 ⟳ shop` (עדיין עובד) ו-`🟨 ✓ wa`.
5. תן לבוט משימה משלו. עכשיו אתה מנהל שניים במקביל, ובמבט אחד על הרשימה בצד אתה
   רואה מי במצב מה.
6. השורה `shop` הופכת ל-`‼ shop` ונשמע צליל — Claude מבקש אישור להתקין חבילה.
   אתה לוחץ על `shop` בצד (לחיצה אחת, הסמן כבר בשורת ההקלדה), מאשר, והוא ממשיך (`⟳ shop`).
7. כשהבוט מסיים, `wa` הופך ל-`✓ wa`. אתה יודע שהתור שלך שם — לוחץ, קורא, וממשיך.
8. סיימת עם `shop` להיום. במקום ללחוץ על הפח, אתה לוחץ **`Alt+Q`**. קופצת שאלה:
   *"לסגור את «shop»?"* — שים לב שהיא **אומרת את שם התיקייה**, אז אתה יודע שאתה
   סוגר את הנכון. לוחץ **"גבה וסגור"**. הטאב הופך ל-`🟨 ⟳ shop` (Claude מגבה),
   בפינה מופיע חיווי התקדמות, וכשהגיבוי נגמר הטאב **נסגר לבד** ומופיעה ההודעה
   `«shop» גובה ונסגר`.
9. **סיימת בהצלחה** כשכל השורות שנשארו ב-`✓` (כולם מחכים לך) ואף אחת לא ב-`‼` (אף
   אחד לא תקוע). ניהלת שני פרויקטים מחלון אחד בלי לפספס כלום — ובלי לאבד אף שיחה.

## טעויות נפוצות ופתרונן
| מה השתבש | מה לעשות |
|----------|----------|
| הרצתי `install.ps1` אבל הטאבים לא בצד / הכותרת לא משתנה | לא פתחת חלון VS Code **חדש**. סגור לגמרי ופתח מחדש. |
| `ccm` לא מוכר ("not recognized") | פתח טרמינל חדש (הפקודה נטענת מחדש), או הקלד `. $PROFILE`. |
| הנוריות (⟳/✓/‼) לא מתחלפות בתוך VS Code | הקלד `$env:CCM_TITLE_MODE="ps"` ופתח חלון חדש; אם עדיין לא, נסה `"tty"`. |
| ריבוע המודל לא מופיע | המודל שלך אינו Opus/Fable/Haiku/Sonnet — רק לארבעה האלה יש ריבוע. (מגרסה `v4` הריבוע מופיע כבר בפתיחת הסשן, לא רק אחרי התור הראשון.) |
| הטאבים נפתחו בלי ריבוע/נורית אחרי שפתחתי מחדש את VS Code | סשנים שנפתחו לפני `v4` נצבעים רק אחרי הפרומפט הראשון. סגור ופתח אותם מחדש, או פשוט שלח פרומפט. |
| `Ctrl+↑/↓` או `Alt+↑/↓` לא מחליף סשן | אתה כנראה לא בתוך הטרמינל אלא ברשימת הטאבים — שם דווקא `↑/↓` לבד עובדים. לחץ פעם אחת בתוך הטרמינל ונסה שוב. |
| `Alt+↑/↓` לא הגיב | עד `v11` רק `Ctrl` היה מחובר. הרץ `install.ps1` שוב — הוא יוסיף את `Alt` (ולא ישכפל את מה שכבר קיים). |
| רוצה את `Ctrl+↑/↓` בחזרה לגלילה בין פקודות | ערוך את `keybindings.json` ומחק את שתי השורות של `ctrl+up`/`ctrl+down`. `Alt+↑/↓` ימשיכו להחליף סשן. |
| כל הטאבים מציגים `‼` בלי סיבה | תוקן בגרסה `v9`. אם זה חוזר — שלח לי את `~/.claude/skills/session-behavior/notifications.log`. |
| לא רוצה שהצבע ישפיע על ה-Explorer | הסר מ-`settings.json` את מפתחות `list.*` שתחת `workbench.colorCustomizations`. יישאר רק הקו בצד. |
| הטאב לא מהבהב באדום | הסשן נפתח לפני שההגדרה נכנסה. סגור ופתח אותו מחדש — היא נקראת רק בעליית סשן. |
| הטרמינל לא עבר למעלה בתיקייה מסוימת | טען מחדש את VS Code באותה תיקייה (`Ctrl+Shift+P` → `Reload Window`). המיקום נשמר לכל תיקייה בנפרד. |
| התקנתי במחשב שני ושום דבר לא עובד | מגרסה `v12` `install.ps1` **רושם את ההוקים בעצמו**. אם התקנת עם גרסה ישנה — הרץ אותו שוב. |
| השם על הטאב ארוך ונחתך | גרור את שפת רשימת הטאבים כדי להרחיב אותה. |
| VS Code שאל אם לבטוח בתיקייה ("Restricted Mode") | לחץ "Trust". זה משפיע רק על פיצ'רים של VS Code, לא על הטרמינל. |
| לחצתי "גבה וסגור" והטרמינל נשאר פתוח | זה בכוונה. הודעה למטה תגיד למה: או ש-Claude שאל אותך משהו (`‼` על הטאב), או שהגיבוי לא הסתיים בזמן. תסתכל בטרמינל, ענה לו, ונסה שוב. |
| לחצתי `Alt+Q` ולא קרה כלום | טען מחדש את VS Code (`Ctrl+Shift+P` → `Reload Window`). לפני הטעינה הקיצור עוד לא מחובר. |
| סגרתי סשן בטעות | תקפוץ הודעה עם **"שחזר את השיחה"** — לחץ עליה. אם כבר סגרת אותה: פתח את התיקייה ב-`Alt+O` והקלד בטרמינל `claude --continue`. |
| הפח כבר לא סוגר בלי לשאול, ואני רוצה שכן | ב-VS Code, `Ctrl+,` → חפש `confirmOnKill` → שנה ל-`never`. התוסף לא ידרוס בחירה מפורשת שלך. |
| סגירת חלון VS Code כבר שואלת "בטוח?", ואני רוצה שתסגור מיד | הכי פשוט: בדיאלוג שקופץ סמן **"Do not ask me again"**. לחלופין ב-`Ctrl+,` חפש `confirmBeforeClose` ושנה ל-`never` (זה השומר הכללי שתמיד שואל), וגם `confirmOnExit` ל-`never` (השומר שפועל כשיש טרמינלים פתוחים). התוסף לא ידרוס בחירה מפורשת שלך. |
| שם הטאב מראה `פרויקט - משהו` ולא רק את שם הפרויקט | זה תקין. ה"משהו" אחרי המקף הוא תת-התיקייה ש-Claude עובד בה כרגע (פירור-לחם). כשהוא חוזר לשורש הפרויקט, המקף נעלם. |

## היסטוריית גרסאות
| תאריך | מה התווסף/השתנה בחוברת |
|--------|------------------------|
| 2026-07-30 | **גרסה `v25` — המאגרים נפתחו לציבור**: ההתקנה במחשב אחר לא דורשת יותר חשבון GitHub, הזמנה או התחברות — משכפלים ומריצים, זהו. נוסף רישיון MIT על שם עידו (מי שמשתמש בקוד מחויב לתת קרדיט). לשנות את הקוד עדיין יכול רק עידו. |
| 2026-07-30 | **גרסה `v24` — חבילה ניידת מלאה**: המתקין האחד (`bootstrap.ps1`) מתקין עכשיו גם את **כלי ההכתבה הקולית**, שואל בעל מחשב חדש אם להדביק **מפתח Gemini משלו** (נשמר מוצפן, רק לכרטיס ההסבר — ההכתבה עובדת בלעדיו), ורושם **עדכון אוטומטי**: כל מחשב מושך את הגרסה האחרונה בכניסה למחשב וכל 12 שעות, בלי לעשות כלום. נוסף מדריך עברית לבעל מחשב חדש — `docs/INSTALL.md`. עודכן סעיף "איך מתחילים". |
| 2026-07-23 | **גרסה `v23` — סייר קבצים צף ב-`Alt+E`**: חלון בסגנון Explorer מעל העבודה — עץ תיקיות בשמאל, תוכן בימין, **ניווט מלא במקלדת** (חצים, `Tab`, `Enter`, הקלדת-אותיות לקפיצה, `Ctrl+F` לסינון), נסגר ב-`Esc`/`✕`/נגיעה במקום אחר. ▶ בכל שורה מריץ את הקובץ (וגם `F5`), תפריט ימני מלא (פתיחה, PDF, נתיב, קובץ/תיקייה חדשים, שינוי-שם, מחיקה לסל), ו**טרמינל של כל סוכן AI בכל תיקייה** — Claude, Codex, Gemini, Copilot, Cursor, opencode, Qwen, Aider; סוכן שלא מותקן מוצע להתקנה בלחיצה. `codex` ו-`gemini` הותקנו במחשב. **דורש ריענון חלון VS Code.** |
| 2026-07-19 | **גרסה `v22` — "לילה אוטונומי"**: (1) נורית הטאב כבר לא משקרת בזמן דחיסת-זיכרון — דחיסה אוטומטית באמצע עבודה מציגה `⟳` ולא `✓` (אפשר להשאיר את Claude לעבוד לילה שלם; הדחיסות קורות לבד). (2) שמירה אוטומטית של קבצים — כתיבה בקובץ המשימות בזמן ש-Claude עובד בטוחה לגמרי, בלי דיאלוג Overwrite. (3) סגירת חלון שואלת שאלה אחת במקום שתיים. (4) בחלון `Alt+O` כל פרויקט מציג כמה משימות פתוחות מחכות בו. **דורש ריענון חלון VS Code.** |
| 2026-07-19 | **`Shift+Enter` יורד שורה בלי לשלוח**: כמו בוואטסאפ — Enter שולח, `Shift+Enter` פותח שורה חדשה בתוך ההודעה. עובד מיד, בלי ריענון. נוסף סעיף "לכתוב הודעה של כמה שורות". |
| 2026-07-17 | **"תמיד תשאל לפני סגירה"**: קודם ה-X של החלון שאל רק כשהיו טרמינלים פתוחים; עכשיו VS Code שואל אישור לפני **כל** סגירת חלון — גם בלי אף טרמינל, וגם בלחיצת עכבר על ה-X. בדיאלוג יש תיבת "אל תשאל שוב" — אם תסמן אותה, זה מכבה את השאלה. **דורש ריענון חלון VS Code.** |
| 2026-07-17 | **הכותרת של הטאב כבר לא "קופצת" לשם תת-תיקייה**: כש-Claude נכנס לתת-תיקייה (למשל `public`), הטאב היה משנה את שמו ומאבד את שם הפרויקט. עכשיו שם הפרויקט **קבוע**, ואם עובדים בתת-תיקייה היא מופיעה אחרי מקף — למשל `קליקיט - public`. |
| 2026-07-14 | **ייצוא Markdown→PDF עם RTL** (כפתור 📄 בסרגל-הכלים של כל קובץ `.md`, וגם בלחיצה-ימנית ב-Explorer): מייצא PDF מיושר-ימין לעברית, אוטומטית. ה-Preview הראה עברית נכון אבל בלי אקספורט, ולעורך אין מצב RTL — הכפתור סוגר את הפער. **ומתקין-על `bootstrap.ps1`** — פקודה אחת שמתקינה/מעדכנת הכל במחשב חדש, כולל Claude עצמו והתוסף; הרצה חוזרת = עדכון. **דורש ריענון חלון VS Code.** |
| 2026-07-13 | **שומר סגירת-חלון**: עד כה לחיצה על ה-X של חלון VS Code סגרה את כל הטרמינלים הפתוחים בבת אחת, בשקט — כולל שיחות Claude חיות. עכשיו, כשיש טרמינלים פתוחים, ה-X שואל אישור לפני שהוא סוגר (מונע סגירה בטעות). הדיאלוג הוא של VS Code עצמו. אפשר לכבות דרך הגדרת `confirmOnExit` (ראה טבלת התקלות). **דורש ריענון חלון של VS Code כדי להיכנס לתוקף.** |
| 2026-07-12 | **תיקון: `Alt+O` ו-`Alt+Q` לא עבדו כשהמקלדת בעברית**. VS Code מזהה קיצור-אות לפי המקש שמפיק את האות בפריסת המקלדת *הפעילה* — ובעברית אין מקש שמפיק "o", אז הקיצור פשוט לא נתפס (בזמן שחיצים כמו `Alt+↑/↓` המשיכו לעבוד, כי אין להם אות). ההגדרה `keyboard.dispatch: keyCode` גורמת ל-VS Code לזהות לפי *מיקום המקש הפיזי* בלי קשר לפריסה, ו-`Alt+O`/`Alt+Q` עובדים בכל שפה. **דורש ריענון חלון של VS Code כדי להיכנס לתוקף.** |
| 2026-07-10 | **הגנה מסגירת טרמינל בטעות**: `Alt+Q` שואל *גבה וסגור / סגור / השאר פתוח*; "גבה וסגור" מריץ `/session-backup` ומחכה שיסתיים; הפח של VS Code מבקש אישור; וסשן שנסגר בטעות מוצע לשחזור עם `claude --continue` |
| 2026-07-10 | **חלון צף חדש ב-`Alt+O`**: רשימת כל הפרויקטים, מסודרת לפי מתי עבדת בכל אחד לאחרונה; בחירה פותחת סשן Claude והחלון נסגר. פרויקט שכבר רץ מסומן `● running` ומקבל פוקוס במקום להיפתח פעמיים |
| 2026-07-09 | `Alt+↑/↓` עובדים גם הם (לא רק `Ctrl`); הטרמינל עולה למעלה בכל תיקייה, לא רק בראשונה; ההתקנה סוף-סוף ניידת למחשב שני |
| 2026-07-09 | הטאב מהבהב באדום כשצריך אותך; הקו של הטאב הפעיל אדום זוהר ולא נעלם לעולם |
| 2026-07-09 | הטאב הנבחר בולט (רקע כחול + קו ענבר); `‼` כבר לא מופיע סתם אחרי דקה של שקט |
| 2026-07-09 | מעבר בין סשנים בלי לחיצה כפולה: לחיצה אחת על טאב, `↑/↓` ברשימה, `Ctrl+↑/↓` בתוך טרמינל — הסמן תמיד נוחת בשורת ההקלדה |
| 2026-07-09 | טאב חדש מציג ריבוע-מודל ונורית `✓` **מיד עם הפתיחה** (קודם הוא נראה ריק עד הפרומפט הראשון); עודכנו התרחיש לדוגמה וטבלת התקלות |
| 2026-07-09 | ריבוע-מודל בכותרת הטאב (עוקב אחרי `/model`); נוריות סטטוס חדשות ⟳/✓/‼; מסגרת על הטאב הפעיל |
| 2026-07-09 | נוספה כניסה בלחיצה ימנית מ-Explorer (תוסף ccm-hub + תפריט); רשימת הטאבים עברה לצד שמאל |
| 2026-07-07 | גרסה ראשונה — התקנה, סיור, תרחיש לדוגמה, טעויות נפוצות |
